const input = document.querySelector("input"); // Esto hace que el selector traiga al input
console.log(input);

console.dir(input); // nos permite inspeccionar el objeto 'input', Es un dirigeme al nodo, nos va a traer el objeto
// Esto nos muestra todas las propiedades y métodos del nodo DOM,
// permitiéndonos acceder y manipularlas con JavaScript.

input.value; // Es para que nos traiga el valor de value
input.value = "Apellido"; // Asi podemos cambiar el valor de la variable en consola
